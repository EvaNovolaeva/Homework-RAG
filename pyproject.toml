[project]
name = "homework-rag"
version = "0.1.0"
description = "RAG homework project"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "scikit-learn>=1.5.0",
    "scipy>=1.13.0",
]

[dependency-groups]
dev = [
    "pytest>=8.0.0",
]
